{{-- Delegate to the plugin-default compare view. Kept as a thin override so the
     system updater preserves the theme's compare view path. To customize the
     layout for this theme, replace this @include with bespoke markup. --}}
@include('plugins/ecommerce::themes.compare')
